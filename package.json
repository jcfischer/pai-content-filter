{
  "name": "@metafactory/content-filter",
  "version": "0.2.0",
  "description": "Inbound content security — layered prompt-injection scanner (regex + heuristic), PII detection, and sandbox enforcement",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "type": "module",
  "private": true,
  "scripts": {
    "check": "bun run src/cli.ts check",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
